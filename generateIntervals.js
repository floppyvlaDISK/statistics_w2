function generateIntervals(coordinatePairs) {
  console.log(typeof coordinatePairs, coordinatePairs);
  const stepX = 0;
  const stepY = 0;
  const intervalsX = [];
  const intervalsY = [];
  return {
    stepX,
    stepY,
    intervalsX,
    intervalsY,
  };
}

module.exports = generateIntervals;
